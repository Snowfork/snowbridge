{
  "name": "@snowbridge/core",
  "private": true,
  "engines": {
    "node": "v18.13.0",
    "pnpm": "7.14.2"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "cd .. && husky install && cd core/packages/test && ./scripts/hack-ethereum.sh && cd ../contracts && forge install",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "size": "turbo run size",
    "coverage": "turbo run coverage"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "turbo": "^1.6.3",
    "pnpm-deduplicate": "^0.4.1"
  }
}
