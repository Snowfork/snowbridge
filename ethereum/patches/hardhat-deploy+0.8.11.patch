diff --git a/node_modules/hardhat-deploy/dist/src/DeploymentsManager.js b/node_modules/hardhat-deploy/dist/src/DeploymentsManager.js
index 0babbb7..e294a2b 100644
--- a/node_modules/hardhat-deploy/dist/src/DeploymentsManager.js
+++ b/node_modules/hardhat-deploy/dist/src/DeploymentsManager.js
@@ -248,7 +248,9 @@ class DeploymentsManager {
         this.networkWasSetup = true;
     }
     async getChainId() {
-        if (this._chainId) {
+        if(this.network.name === "kintsugi") {
+            this._chainId = this.network.config.chainId;
+            this.setupNetwork();
             return this._chainId;
         }
         this.setupNetwork();
@@ -384,7 +386,7 @@ class DeploymentsManager {
         if (name.includes('/') || name.includes(':')) {
             throw new Error(`deployment name must not be a path or Fully Qualified Name - for such purposes consider using the "contract" field of deployment options`);
         }
-        const chainId = await this.getChainId();
+        const chainId = (await this.getChainId()).toString();
         const toSave = this.db.writeDeploymentsToFiles && this.network.saveDeployments;
         const filepath = path_1.default.join(this.deploymentsPath, this.deploymentFolder(), name + '.json');
         // handle ethers receipt :
